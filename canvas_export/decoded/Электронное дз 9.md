# Электронное дз 9

Всего заданий: **3**.

## 1. 9.1

**Тип:** Множественный выбор

Выберите верные утверждения:

- ☐ Любую схему стойкого аутентифицированного шифрования можно использовать в качестве стойкого кода аутентичности
- ☐ При использовании схемы MAC-then-Encrypt необходимо использовать независимые ключи для MAC и шифрования
- ☐ При использовании схемы EAX необходимо использовать независимые ключи для MAC и шифрования
- ☐ CCA стойкость более сильно определение, чем CPA стойкость
- ☐ Возможно построить CCA стойкий шифр, который не будет CPA стойким
- ☐ Обеспечение целостности открытых текстов не может быть обеспечено через целостность соответствующих шифртекстов
- ☐ Схема Encrypt-and-MAC в общем случае является нестойкой
- ☐ CCA стойкости достаточно для защиты аутентичности от пассивных противников
- ☐ Целостность шифртекстов более сильное определение, чем целостность открытых текстов, при передаче шифртекстов по каналу связи

## 2. 9.2.l

**Тип:** Одиночный выбор

Пусть \( k \in_r K \) – случайная величина, полученная с использованием неравномерного распределения, \( K=\{0,1\}^{256} \): \( \forall c \in \{0,1\}^{256}: Pr[k=c] = \) if \( MSB_{128}(c)==0^{128} \{ 2^{-128} \} \) else \( \{ 0 \} \) Иными словами, \(k \) выбирается из подмножества векторов в \( K \), для которых первые 128 бит – нулевые. Пусть \( F(k,x) \) – стойкая PRF, с множеством ключей \( K \). Какие из PRF ниже является стойкими PRF (в практическом смысле, минимальный параметр стойкость – 80 бит), но не является стойкими при выборе \( k \) c использованием распределения, описанного выше?

- ☐ \( F' (k,x)=if MSB_{128}(k)\neq 0^{128} \{ F(k,x) \}; else \{ 0^{256} \} \)
- ☐ \( F' (k,x)=if MSB_{128}(k)\neq 1^{128} \{ F(k,x) \}; else \{ 0^{256} \} \)
- ☐ \( F' (k,x)=F(k,x) \)
- ☐ \( F' (k,x)=if MSB_{128}(k)\neq 1^{128} \{ F(k,x) \}; else \{ 1^{256} \} \)

## 3. 9.4.l

**Тип:** Загрузка файла

Пусть \( (E,D) \) – строго стойкий блочный шифр на \( (K,M \times R),1/|R| \) - пренебрежимо малая. Докажите, что шифр ниже стойкий CCA шифр, но не стойкий AE шифр (не обеспечивается целостность шифртекстов). Строго стойкий блочных шифр (strongly secure block cipher) – шифр, стойкий против противников, которые могут помимо запросов на зашифрование произвольных блоков запрашивать запросы на расшифрование произвольных блоков блочного шифра в игре на стойкость блочного шифра (PRP). \( E' (k,m)={r←^R R,c←^R E(k,(m,r)),return \{ c \}} \) \( D' (k,c)={(m,r' )←D(k,c),return \{ m \}} \)

**Ответ:** прикрепить файл.

---

## Статистика

- Множественный выбор: 1
- Одиночный выбор: 1
- Загрузка файла: 1
- Всего вариантов в вопросах с выбором: 13
