# Электронное дз 6

Всего заданий: **6**.

## 1. 6.1.a

**Тип:** Загрузка файла

В некоторой криптосистеме используется блочный шифр в детерминированном режиме CTR. Предполагая стойкость блочного шифра с функцией зашифрования \( E \), является ли описанная криптосистема стойкой при одноразовом использовании ключа в теоретическом (предельном) смысле? (записать в ответ да или нет) Почему? (на доп листах)

**Ответ:** прикрепить файл.

## 2. 6.1.b

**Тип:** Загрузка файла

В некоторой криптосистеме используется блочный шифр в детерминированном режиме CTR. Предполагая стойкость блочного шифра с функцией зашифрования \( E \), является ли описанная криптосистема стойкой при многоразовом использовании ключа (ключ используется для шифрования нескольких сообщений) в теоретическом (предельном) смысле? (записать в ответ да или нет) Почему? (на доп листах)

**Ответ:** прикрепить файл.

## 3. 6.1.с

**Тип:** Загрузка файла

В некоторой криптосистеме используется блочный шифр в детерминированном режиме CTR. Пусть в качестве \( E \) используется PRP, с длинной ключа 128 бит, размер блока 128 бит, параметр стойкости принять равным 128 бит. Пусть имеется защищенный канал связи с пропускной способностью 100 mebibit/s, в котором непрерывно шифруются сообщения.  Оценить вероятность (степенью 2) успешной атаки на криптосистему в течении одного года, при условии, что симметричный ключ не меняется. * при вычислениях полагать что шифруется единственное сообщение максимальной длинны, которое может быть передано в указанном канале за заданное время.

**Ответ:** прикрепить файл.

## 4. 6.1.d

**Тип:** Загрузка файла

В некоторой криптосистеме используется блочный шифр в детерминированном режиме CTR. Пусть в качестве \( E \)  используется PRP, с длинной ключа 128 бит, размер блока 64 бит, параметр стойкости принять равным 120 бит. Пусть имеется защищенный канал связи с пропускной способностью 100 mebibit/s, в котором непрерывно шифруются сообщения. Оценить необходимую частоту смены симметричного ключа, при заданной вероятности атаки равной \( 2^{-7} \). * при вычислениях полагать что шифруется единственное сообщение максимальной длинны, которое может быть передано в указанном канале за заданное время.

**Ответ:** прикрепить файл.

## 5. 2

**Тип:** Загрузка файла

После анализа симметричной криптосистемы была получена следующая оценка стойкости в сведении к псевдослучайной функции: $$Adv[A,C]≤tn/N(tQ/N+Adv_{prf}[B,E])$$ где  \( E \)– функция зашифрования блочного шифра,  \( Q \) – максимальное число обращений к криптосистеме при фиксированном ключе,  \(N=2^n\) – размер блока PRF, \(t \) – размер выхода криптосистемы. Предполагая стойкость блочного шифра с функцией зашифрования \(E\), является ли описанная криптосистема стойкой в теоретическом (предельном) смысле? (записать в ответ да или нет) Почему? (на доп листах)

**Ответ:** прикрепить файл.

## 6. 3

**Тип:** Множественный выбор

Выберите верные утверждения:

- ☐ Любая PRP является PRF
- ☐ Любая PRF является PRP
- ☐ Любая стойкая PRF является PRP
- ☐ Любая стойкая PRP является стойкой PRF
- ☐ Любая стойкая PRP со сверх-полиномиальным образом является стойкой PRF
- ☐ Любой стойкий блочный шифр является стойкой PRF
- ☐ Любой семантически стойкий шифр (одноразовое использование ключа) должен быть детерминированным
- ☐ Любой CPA стойкий шифр является семантически стойким при одноразовом использовании ключа.

---

## Статистика

- Загрузка файла: 5
- Множественный выбор: 1
- Всего вариантов в вопросах с выбором: 8
